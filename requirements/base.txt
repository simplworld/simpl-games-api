# Wheel 0.25+ needed to install certain packages on CPython 3.5+
# like Pillow and psycopg2
# See http://bitly.com/wheel-building-fails-CPython-35
# Verified bug on Python 3.5.1
wheel==0.29.0

# Django
django==1.9.6

# Configuration
django-environ==0.4.0
django-secure==1.0.1
whitenoise==2.0.6

# Forms
django-braces==1.8.1
django-crispy-forms==1.6.0
django-floppyforms==1.6.1

# Models
django-model-utils==2.4

# Images
Pillow==3.1.1

# For user registration, either via email or social
# Well-built with regular release cycles!
django-allauth==0.24.1

# Python-PostgreSQL Database Adapter
psycopg2==2.6.1

# Unicode slugification
django-autoslug==1.9.3

# Time zones support
pytz==2015.7

# Redis support
django-redis==4.3.0
redis>=2.10.0

# Celery support
celery==3.1.21

# Wharton styles
git+https://github.com/jefftriplett/django-base-theme.git@432f87f  # Pinned to the latest
git+https://github.com/dyve/django-bootstrap3.git@1ad2686  # Pin to 7.0.1 once released (currently at 6.2.2)

# Custom Wharton apps
# since the salt states need these repos to use the protocol prefix, `git+https` 
# and the devs need `git+ssh`, `django_courses` and `simpl-users` repositories 
# have been moved from base.txt to requirements/local.txt and 
# requirements/salt.txt respectively

django-filter==0.13.0
django-rest-swagger==0.3.7
djangorestframework==3.3.3

thorn==1.5.0

# Nicer CLI Interface
click==6.6
django-click==1.2.0

# for courses
unicodecsv==0.14.1
django-fontawesome==0.2.2
